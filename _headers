# Prevent caching so visitors always get the latest version after each deploy.
# Cloudflare Pages applies these headers; use "Purge Everything" in Cloudflare
# Dashboard (Caching) once after changing this if the site still looks stale.

/*
  Cache-Control: no-store, no-cache, must-revalidate, max-age=0
  Pragma: no-cache
  Expires: 0
